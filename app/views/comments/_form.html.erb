<%= form_with(model: [@post, @comment], url: post_comments_path(@post),class:"mb-3" ) do |f| %>
    <%= f.text_area :content, class: "input-mysize" %>
    <%= f.submit "送信", class: "btn btn-primary comment-submit float-right" %>
<% end %>